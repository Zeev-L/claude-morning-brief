# local runtime state — contains the email webhook URL (a secret) and brief content
state/
# local logs
logs/
# built URL-scheme handler app (rebuilt by install.sh from the .applescript source)
ClaudeResume.app/
# macOS
.DS_Store
